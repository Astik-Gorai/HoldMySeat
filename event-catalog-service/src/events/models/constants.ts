export enum EventType {
    MOVIE = 'movie',
    CONCERT = 'concert',
    SPORTS = 'sports',
    STANDUP = 'standup'
}

export enum EventStatus {
    DRAFT = 'draft',
    PUBLISHED = 'published',
    CANCELLED = 'cancelled',
}

export enum ShowStatus {
    CANCELLED = 'cancelled',
    RUNNING = 'running',
    HOUSEFULL  = 'housefull',
    PENDING = 'pending'
}